#include "check.h"
#include "srpc/types.h"

srpc_check_status_t interfaces_check_interface(interfaces_ctx_t* ctx, const interfaces_interface_hash_element_t* if_hash)
{
    srpc_check_status_t status = srpc_check_status_non_existant;
    return status;
}
